# --- Core framework ---------------------------------------------------
Django>=5.1,<5.2
django-environ==0.11.2
django-htmx==1.19.0

# --- Document parsing (spec: Supported Documents) ----------------------
pypdf==4.3.1                # PDF text extraction
python-docx==1.1.2          # DOCX
docx2txt==0.8               # fallback DOCX/DOC text extraction
openpyxl==3.1.5             # XLSX
xlrd==2.0.1                 # legacy XLS
pandas==2.2.3                # CSV / tabular normalization
beautifulsoup4==4.12.3      # HTML parsing
lxml==5.3.0
striprtf==0.0.26            # RTF
python-pptx==1.0.2          # PPTX

# --- OCR (spec: OCR Engine) --------------------------------------------
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0

# --- Embeddings / LLM (spec: Ollama-only, fully offline) ----------------
ollama
#==0.4.4

# --- Vector database (spec: FAISS or ChromaDB, abstracted) --------------
chromadb==0.5.23
faiss-cpu==1.11.0

# --- Chunking / NLP -------------------------------------------------------
nltk==3.9.1
tiktoken==0.8.0             # token-accurate chunk sizing

# --- Background / async processing (spec: Async document processing) ----
django-q2==1.7.4            # pure-Python task queue, no Redis/Celery required on Windows

# --- API layer (spec: REST APIs) ----------------------------------------
djangorestframework==3.15.2

# --- Security / utilities -------------------------------------------------
django-axes
#==6.5.3          # brute-force login protection
python-magic-bin==0.4.14; sys_platform == "win32"   # file-type validation on Windows
python-magic==0.4.27; sys_platform != "win32"

# --- Testing (spec: Testing section) --------------------------------------
pytest==8.3.4
pytest-django==4.9.0
factory-boy==3.3.1
